apiVersion: apps/v1
kind: Deployment
metadata:
  name: cf-llm-multi-agent-frontend
  namespace: cloudflared
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cf-llm-multi-agent-frontend
  template:
    metadata:
      labels:
        app: cf-llm-multi-agent-frontend
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --no-autoupdate
            - --protocol
            - http2
            - --url
            - http://llm-multi-agent-frontend.apps.svc.cluster.local:80
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
